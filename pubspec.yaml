name: flutter_payments
description: Siltium plugin to pay
version: 0.3.3
homepage: https://github.com/siltium-labs/flutter-payments/tree/development

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8
  http: ^1.2.2
  mvc_pattern: ^8.12.0
  webview_flutter: ^4.8.0
  url_launcher: ^6.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.siltium.flutter_payments
        pluginClass: FlutterPaymentsPlugin
      ios:
        pluginClass: FlutterPaymentsPlugin
      linux:
        pluginClass: FlutterPaymentsPlugin
      macos:
        pluginClass: FlutterPaymentsPlugin
      windows:
        pluginClass: FlutterPaymentsPluginCApi

  # To add assets to your plugin package, add an assets section, like this:
  
  assets:
    - icons/
