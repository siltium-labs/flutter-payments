/* import 'package:flutter_test/flutter_test.dart';
import 'package:multipay_plugin/multipay_plugin_platform_interface.dart';
import 'package:plugin_platform_interface/plugin_platform_interface.dart';

class MockMultipayPluginPlatform
    with MockPlatformInterfaceMixin
    implements MultipayPluginPlatform {
  @override
  Future<String?> getPlatformVersion() => Future.value('42');

  @override
  Future startMPCheckout() {
    // TODO: implement startMPCheckout
    throw UnimplementedError();
  }
}

void main() {
  /* final MultipayPluginPlatform initialPlatform =
      MultipayPluginPlatform.instance;

  test('$MethodChannelMultipayPlugin is the default instance', () {
    expect(initialPlatform, isInstanceOf<MethodChannelMultipayPlugin>());
  });

  test('getPlatformVersion', () async {
    MultipayPlugin multipayPlugin = MultipayPlugin();
    MockMultipayPluginPlatform fakePlatform = MockMultipayPluginPlatform();
    MultipayPluginPlatform.instance = fakePlatform;

    expect(await multipayPlugin.getPlatformVersion(), '42');
  }); */
}
 */